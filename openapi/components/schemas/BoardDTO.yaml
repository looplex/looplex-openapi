title: BoardDTO
type: object
properties:
  bindings:
    type: array
    uniqueItems: true
    description: The list of bindings from the retrieved tasks.
    items:
      $ref: "./Binding.yaml"
  ids:
    type: array
    description: The ids of the retrieved tasks.
    items:
      type: integer
      format: int64
  tasks:
    type: array
    items:
      $ref: "./Task.yaml"
  thirdPartyEntities:
    type: array
    items:
      $ref: "./ThirdPartyEntity.yaml"
description: A binding represents an association between a task and a third party entity.