tags: 
  - Session (Lawsoft)
summary: Login web
description: Efetua o login (Lawsoft web)
operationId: api-websession
parameters:
  - $ref: ../../../components/parameters/lawsoft/SubscriptionKey.yaml
requestBody:
  content:
    application/json:
      schema:
        $ref: ../../../components/parameters/lawsoft/LoginWeb.yaml
responses:
  "200":
    description: "Efetua o login (Lawsoft web)"
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/DTOLogonResult.yaml
  "401":
    description: "Em caso de erro de timeout ou sessão expirada."
  "500":
    description: "Em caso de erro diferente de timeout ou sessão expirada."
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/InternalServerErrorResponse.yaml
