tags: 
  - Conta de acesso
summary: Conta de acesso por nome
description: Obtém a conta de acesso por nome
operationId: api-ContaDeAcesso-ObtemContaDeAcesso
parameters:
  - $ref: ../../../components/parameters/lawsoft/SubscriptionKey.yaml
  - name: nomeUsuario
    in: query
    required: true
    schema:
      type: string
  - name: domain
    in: query
    required: true
    schema:
      type: string
responses:
  "200":
    description: "Obtém a conta de acesso por nome"
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/DTOContaDeAcesso.yaml
  "401":
    description: "Em caso de erro de timeout ou sessão expirada."
  "500":
    description: "Em caso de erro diferente de timeout ou sessão expirada."
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/InternalServerErrorResponse.yaml
