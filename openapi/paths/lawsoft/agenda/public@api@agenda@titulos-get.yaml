tags: 
  - Agenda 
summary: Títulos de agenda
description: Obtém os títulos de agenda.
operationId: api-agenda-titulos
parameters:
  - $ref: ../../../components/parameters/lawsoft/Profile.yaml
  - $ref: ../../../components/parameters/lawsoft/SubscriptionKey.yaml
  - name: tipo
    in: query
    required: true
    schema:
      $ref: ../../../components/schemas/lawsoft/enums/TipoItemAgenda.yaml
responses:
  "200":
    description: Retorna um array com os títulos de agenda.
    content:
      application/json:
        schema: 
          type: array
          items:
            $ref: ../../../components/schemas/lawsoft/DTOTituloAgenda.yaml
  "401":
    description: Em caso de erro de timeout ou sessão expirada.
  "500":
    description: Em caso de erro diferente de timeout ou sessão expirada.
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/InternalServerErrorResponse.yaml
