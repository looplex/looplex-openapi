tags: 
  - Agenda (Lawsoft)
summary: Obtém participantes
description: Obtém os participantes.
operationId: api-agenda-participantes
parameters:
  - $ref: ../../../components/parameters/lawsoft/Profile.yaml
  - $ref: ../../../components/parameters/lawsoft/SubscriptionKey.yaml
responses:
  "200":
    description: Retorna um array com os participantes.
    content:
      application/json:
        schema: 
          type: array
          items:
            $ref: ../../../components/schemas/lawsoft/DTOFuncionario.yaml
  "401":
    description: Em caso de erro de timeout ou sessão expirada.
  "500":
    description: Em caso de erro diferente de timeout ou sessão expirada.
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/InternalServerErrorResponse.yaml
