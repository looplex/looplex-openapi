tags: 
  - Funcionário 
summary: Funcionário
description: Obtém as informações do funcionário de acordo com o identificador informado
operationId: employeeByIdentifier
parameters:
  - $ref: ../../../components/parameters/lawsoft/Profile.yaml
  - $ref: ../../../components/parameters/OcpApimSubscriptionKey.yaml
  - name: id
    in: path
    example: "10"
    required: true
    schema:
      type: integer
responses:
  "200":
    description: "Obtém as informações do funcionário de acordo com o identificador informado"
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/DTOFuncionario.yaml
  "401":
    description: "Em caso de erro de timeout ou sessão expirada."
  "500":
    description: "Em caso de erro diferente de timeout ou sessão expirada."
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/InternalServerErrorResponse.yaml
