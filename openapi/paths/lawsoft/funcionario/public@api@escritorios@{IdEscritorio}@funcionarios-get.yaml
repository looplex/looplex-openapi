tags: 
  - Funcionário (Lawsoft)
summary: Funcionários do escritório
description: Obtém as informações dos funcionários de acordo com o escritório informado
operationId: api-escritorios-IdEscritorio-funcionarios
parameters:
  - $ref: ../../../components/parameters/lawsoft/Profile.yaml
  - $ref: ../../../components/parameters/lawsoft/SubscriptionKey.yaml
  - name: IdEscritorio
    in: path
    required: true
    schema:
      type: integer
responses:
  "200":
    description: "Obtém as informações dos funcionários de acordo com o escritório informado"
    content:
      application/json:
        schema: 
          type: array
          items:
            $ref: ../../../components/schemas/lawsoft/DTOFuncionarioBase.yaml
  "401":
    description: "Em caso de erro de timeout ou sessão expirada."
  "500":
    description: "Em caso de erro diferente de timeout ou sessão expirada."
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/InternalServerErrorResponse.yaml
