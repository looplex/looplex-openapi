# Linhas com # são comentários e podem ser removidas.
tags:
  - Varas e Foros
summary: Lista ritos e grupos de ritos
operationId: api-VF-ListaRitosGrupoRitos
# Parâmetros da requisição
parameters:
  - $ref: ../../../components/parameters/lawsoft/SubscriptionKey.yaml
  - name: tipoPesquisa
    in: query
    required: true
    schema:
      $ref: ../../../components/schemas/lawsoft/enums/TipoPesquisaGrupoRito.yaml
  - name: identificador
    in: query
    example: 15
    description: "o identificador do item"
    required: true
    schema:
      type: integer
# RequestBody aqui (se houver). Verificar menu "Copiar body da requisição"
# Responses
responses:
  # Adicione aqui outras respostas HTTP. Verificar menu "Copiar nova resposta HTTP"
  "200":
    description: "Response"
    content:
      application/json:
        schema:
          $ref: ../../../components/schemas/lawsoft/ResponseListaRitosGrupoRitos.yaml