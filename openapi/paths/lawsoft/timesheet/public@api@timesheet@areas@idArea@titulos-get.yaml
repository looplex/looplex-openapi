tags: 
  - Timesheet 
summary: Títulos para timesheet
description: Obtém a lista de títulos para lançamentos de hora
operationId: api-timesheet-areas-idArea-titulos
parameters:
  - $ref: ../../../components/parameters/lawsoft/Profile.yaml
  - $ref: ../../../components/parameters/lawsoft/SubscriptionKey.yaml
  - name: idArea
    in: path
    required: true
    schema:
      type: integer
responses:
  "200":
    description: "Obtém a lista de títulos para lançamentos de hora"
    content:
      application/json:
        schema: 
          type: array
          items:
            $ref: ../../../components/schemas/lawsoft/DTOClassificacao.yaml
  "401":
    description: "Em caso de erro de timeout ou sessão expirada."
  "500":
    description: "Em caso de erro diferente de timeout ou sessão expirada."
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/InternalServerErrorResponse.yaml
