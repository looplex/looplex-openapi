tags: 
  - Cliente 
summary: Ficha do cliente (Mobile)
description: Obtém a ficha do cliente para o mobile
operationId: api-clientes-ficha-mobile
parameters:
  - $ref: ../../../components/parameters/lawsoft/Profile.yaml
  - $ref: ../../../components/parameters/OcpApimSubscriptionKey.yaml
  - name: idCliente
    in: query
    required: true
    schema:
      type: integer
responses:
  "200":
    description: "Obtém a ficha do cliente para o mobile"
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/DTOClienteMobile.yaml
  "401":
    description: "Em caso de erro de timeout ou sessão expirada."
  "500":
    description: "Em caso de erro diferente de timeout ou sessão expirada."
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/lawsoft/ExceptionResponse.yaml
