# Linhas com # são comentários e podem ser removidas.
tags:
  - Garantia
summary: Detalhes da garantia
description: Obtém os detalhes da garantia
operationId: api-casos-Garantia-idCasoGarantia
# Parâmetros da requisição
parameters:
  - $ref: ../../../components/parameters/cases/Profile.yaml
  - $ref: ../../../components/parameters/OcpApimSubscriptionKey.yaml
  - name: idCasoGarantia
    in: path
    description: "O identificador da garantia"
    required: true
    schema:
      type: integer
# RequestBody aqui (se houver). Verificar menu "Copiar body da requisição"
# Responses
responses:
  # Adicione aqui outras respostas HTTP. Verificar menu "Copiar nova resposta HTTP"
  "200":
    description: "Detalhes da garantia"
    content:
      application/json:
        schema:
          $ref: ../../../components/schemas/cases/DTOCasoGarantia.yaml
  "401":
    description: "Em caso de erro de timeout ou sessão expirada."
  "500":
    description: "Em caso de erro diferente de timeout ou sessão expirada."
    content:
      application/json:
        schema:
          $ref: ../../../components/schemas/cases/ExceptionResponse.yaml