tags: 
  - Hierarquia 
summary: Grupo foro
description: Obtém as informações do grupo foro
operationId: api-hierarquia-GrupoForoDetalhado
parameters:
  - $ref: ../../../components/parameters/cases/Profile.yaml
  - $ref: ../../../components/parameters/OcpApimSubscriptionKey.yaml
  - name: cdGrupoForo
    in: query
    required: true
    schema:
      type: integer
  - name: cdUF
    in: query
    required: true
    schema:
      type: string
responses:
  "200":
    description: "Obtém as informações do grupo foro"
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/cases/DTOGrupoForo.yaml
  "401":
    description: "Em caso de erro de timeout ou sessão expirada."
  "500":
    description: "Em caso de erro diferente de timeout ou sessão expirada."
    content:
      application/json:
        schema: 
          $ref: ../../../components/schemas/cases/ExceptionResponse.yaml
