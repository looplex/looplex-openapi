tags:
- Bindings
summary: Bind
description: Bind a task to an entity by their IDs
operationId: addBindingUsingPOST
requestBody:
  content:
    application/json:
      schema:
        type: array
        items:
          $ref: ../components/schemas/Tasks/Binding.yaml
responses:
  '200':
    description: OK